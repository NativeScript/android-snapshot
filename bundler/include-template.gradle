android {
    productFlavors {
        "${ORIGINAL_NAME}-snapshot" {
            dimension "${ORIGINAL_NAME}-snapshot"
        }
    }
}

android {
    aaptOptions {
        ignoreAssetsPattern "_embedded_script_.js"
    }
}
